import { axiosInstance } from "./instances"

export const reportsAPI = (data) => {
    try {
        console.log("inside api function..")
        // let response = axiosInstance.post(
        //     "",
        //     data
        // )
        // return response;
        return [
            "abc", "def"
        ]
    }
    catch (error) {

    }
}

export const allDataAPI = (data) => {
    try {
        console.log("inside api function..")
        // let response = axiosInstance.post(
        //     "",
        //     data
        // )
        // return response;
        return [
            "abc", "def"
        ]
    }
    catch (error) {

    }
}